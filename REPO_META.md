# GitHub 仓库元信息（建仓库时复制粘贴用）

## 仓库名

`cys-prompt-suite`

## 描述（Description）

中文 AI 提示词生成 + 平台合规校验 一体化 MCP —— 「生成即合规」闭环，内置 8800+ 条扩展词库。

## 一屏简介（About · 长描述）

> 把「写实人像 / 动漫角色 / 海螺3 视频」三类中文提示词生成与「抖音/快手/视频号/小红书」平台合规护栏，
> 合成一个 stdio MCP 服务：生成 → 自动合规校验 → 命中红线自动清洗复检 → 返回可直接发布的安全提示词与自检清单。
> 词库来自主人自有技能 references（6 个 md 词表 + 结构化词库，均为事实性词汇），可复现、可商用。

## Topics（标签）

`mcp` `model-context-protocol` `ai-prompt` `prompt-engineering` `compliance`
`chinese-ai` `claude-desktop` `cursor` `comfyui` `hailuo` `fastmcp` `python`

## 主页/引用（可选）

- 词库溯源与可商用说明：见 `README.md` §6
- 客户端接入：见 `mcp-config/README.md`
